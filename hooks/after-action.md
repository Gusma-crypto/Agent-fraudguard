# After Action

> **Enforcement status:** active through agent output guards and Core validation/audit.
> Native organizer hook registration remains pending
> verification on the VPS.

Every tool result must have a Core trace ID. Agent logs record routing only; Core owns
append-oriented authoritative audit. Output guards redact configured secrets and never
present agent inference as a protected decision.
Respons chat harus melaporkan tindakan tercatat lewat `actions[]`; kegagalan side effect
setelah analisis wajib fail closed dan dieskalasikan.
