# Apply Without Overwriting

> **Current workspace status:** approved additions have already been merged into the active root `AGENTS.md`, `SOUL.md`, and `TOOLS.md`. `HEARTBEAT.md` is intentionally comments-only. Keep this directory as merge history; do not reapply it blindly.
1. Keep current root OpenClaw files unchanged.
2. Copy new directories/files only.
3. Review `*.append.md` manually.
4. Append approved sections to existing files.
5. Never copy `reference/openclaw-current/*` over the workspace.
6. `BOOTSTRAP.md` and `IDENTITY.md` are intentionally not modified.
